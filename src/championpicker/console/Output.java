package championpicker.console;

public class Output {
    // Later replace with a gui msg box instead of terminal
    public static void err(String errmsg) {
        System.err.println(errmsg);
    }
}
